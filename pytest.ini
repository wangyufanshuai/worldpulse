[pytest]
asyncio_default_fixture_loop_scope = function
markers =
    postgres_live: requires WORLDPULSE_TEST_POSTGRES_URL and a live PostgreSQL database
    evaluation_full: runs the 414-member Mock release gate and requires an imported official benchmark and sealed label pack
